Before:
  call ale#assert#SetUpLinterTest('verilog', 'verible')

After:
  call ale#assert#TearDownLinterTest()

Execute(The default verible command should be correct):
  AssertLinter 'verible', 'verible-verilog-lint %t'

Execute(verible options should be configurable):
  " Additional args for the linter
  let g:ale_verilog_verible_options = '--ruleset=all %t'

  AssertLinter 'verible', 'verible-verilog-lint --ruleset=all %t'
